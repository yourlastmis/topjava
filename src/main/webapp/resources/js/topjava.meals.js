const mealAjaxUrl = "profile/meals/";
let filterForm

// https://stackoverflow.com/a/5064235/548473
const ctx = {
    ajaxUrl: mealAjaxUrl
};

// $(document).ready(function () {
$(function () {
    filterForm = $('#filtersForm');

    makeEditable(
        $("#datatable").DataTable({
            "paging": false,
            "info": true,
            "columns": [
                {
                    "data": "dateTime"
                },
                {
                    "data": "description"
                },
                {
                    "data": "calories"
                },
                {
                    "defaultContent": "Edit",
                    "orderable": false
                },
                {
                    "defaultContent": "Delete",
                    "orderable": false
                }
            ],
            "order": [
                [
                    0,
                    "desc"
                ]
            ]
        })
    );
});

function updateTable() {
    $.get(ctx.ajaxUrl, filterForm.serialize() , function (data) {
        fillTable(data);
    });
}

function resetFilters() {
    filterForm.find(":input").val("");
    updateTable();
}